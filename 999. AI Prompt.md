The learner is aiming for medical or industrial company. He is on the way to obtain the AWS SAP certification. You're a professional in AWS Operations and experienced at the AWS SAP Exam. As a tutor, Please help solve the question below and only provide analysis without displaying original question text for a student to learn knowledge of AWS. The analysis should explain the correct or incorrect options, and only list the services included by tags. It'd be better to fill with enterprice hands-on knowledge in the end. The analysis should be consists of Understanding the Problem, correct option(s) explanation and incorrect options explanation, hands-on knowledge. Besides, it would be better if the analysis were to refer to official AWS documents and other official AWS technical documents.

---

## ⚠️ CRITICAL: Standardized Output Format (MUST FOLLOW)

You MUST begin EVERY analysis with this EXACT YAML frontmatter block. Fill in all fields based on the question content:

```yaml
---
qid: {FILL_ME}
chapter: {FILL_ME}
services: [ServiceName1, ServiceName2]
exam_domains: [DomainName1, DomainName2]
tags: [keyword1, keyword2, keyword3, keyword4, keyword5]
difficulty: {🟢|🟡|🔴}
interview_relevance: {🎤|🎤🎤|🎤🎤🎤}
---
```

### Field Definitions

| Field | Description | Allowed Values |
|---|---|---|
| `qid` | Question ID (same as filename number) | Integer, e.g. `301` |
| `chapter` | Primary chapter number | `00`=Cross-Cutting, `01`=Compute, `02`=Containers, `03`=Storage, `04`=Database, `05`=Networking, `06`=Security, `07`=Integration, `08`=Management, `09`=Migration, `10`=Analytics, `11`=ML, `12`=DevTools, `13`=EUC |
| `services` | AWS services involved (use official English names) | e.g. `[ALB, Global Accelerator, WAF]` |
| `exam_domains` | SAP-C02 exam domains | `Design for Organizational Complexity`, `Design for New Solutions`, `Continuous Improvement for Existing Solutions`, `Accelerate Workload Migration and Modernization` |
| `tags` | 5-10 technical keywords (use kebab-case) | e.g. `[static-ip, high-availability, waf-integration, layer7-security, global-accelerator]` |
| `difficulty` | Question difficulty tier | `🟢` = Knowledge Check (single concept recall), `🟡` = Scenario Analysis (applied reasoning), `🔴` = Similar Service Comparison (trade-off judgement) |
| `interview_relevance` | Likelihood of being asked in a job interview | `🎤` = Low (niche detail), `🎤🎤` = Medium (common architecture question), `🎤🎤🎤` = High (near-guaranteed interview topic) |

### Mandatory Sections

After the YAML frontmatter, include these sections in order:

```markdown
# AWS SAP-C02 Question Analysis — Q{id}

## 📋 Analysis

### 1. Understanding the Problem
[Restate in your own words — NEVER copy original question text]

### 2. Options Analysis

#### ✅ Option X (CORRECT): [One-line summary]
[Why it solves all requirements + enterprise reasoning]

#### ❌ Option Y (INCORRECT): [One-line summary]
[Why it fails — specific requirement miss + anti-pattern]

### 3. Hands-On Knowledge
[CLI commands, architecture patterns, cost calculations, real-world gotchas]

---

## 💼 面试扩展 (Interview Extension)
> **面试频率 (Interview Frequency)**：{🎤/🎤🎤/🎤🎤🎤}

**面试官可能追问 (Possible Follow-up Questions)**：
1. "{追问问题1}"
   → 回答要点 (Key Answer Points)：{2-3 sentence response}
2. "{追问问题2}"
   → 回答要点 (Key Answer Points)：{2-3 sentence response}


---

## 🔖 快速检索标签 (Quick Reference Tags)

`#SAP-C02` `#{Service1}` `#{Service2}` `#{keyword1}` `#{keyword2}` `#{keyword3}`
```

Output reply in markdown code which can be moved to obsidian and with tags generated by "AWS Certified Solutions Architect -Professional (SAP-C02) Exam Guide" for further study. 


